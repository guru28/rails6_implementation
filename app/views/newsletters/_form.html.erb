<%= form_for @newsletter, html:{multipart: true } do |f| %>
 
  <% if @newsletter.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@newsletter.errors.count, "error") %> prohibited
        this newsletter from being saved:
      </h2>
      <ul>
        <% @newsletter.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :subject %><br>
    <%= f.text_field :subject %>
  </p>
  
  <p>
    <%= f.label :from_address %><br>
    <%= f.text_field :from_address %>
  </p>

  <p>
    <%= f.label :content %><br>
    <%= f.rich_text_area :content %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
